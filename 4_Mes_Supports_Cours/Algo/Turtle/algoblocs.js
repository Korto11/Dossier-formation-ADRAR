/*
// Série A
// A1
// Initialisation
setPos(300,300);
changeColor(color.red);
setLineWidth(10);

// Réalisation
forward(200);
faceRight();
forward(100);
*/

/*
// A2
// Initialisation
setPos(300,300);
changeColor(color.yellow);
setLineWidth(10);
faceLeft();

// Réalisation
forward(100);
faceDown();
forward(100);
faceRight();
forward(200);
faceUp();
forward(200);
*/

/*
// A3
// Initialisation
setPos(300,300);
changeColor(color.red);
setLineWidth(10);
faceLeft();

//Réalisation
forward(100);
faceRight();
forward(200);
faceLeft();
forward(100);
faceUp();
forward(100);
faceDown();
forward(200);
*/

/*
// A4
// Initialisation
setPos(100,500);
changeColor(color.green);
setLineWidth(10);
faceRight();

//Réalisation
forward(200);
arcLeft(100,180);
forward(100);
arcRight(100,180);
forward(200);
*/

/*
// A5
// Initialisation
setPos(300,200);
changeColor(color.fuchsia);
setLineWidth(10);
faceRight();

// Réalisation
arcRight(100,360);
arcRight(50,180);
arcLeft(50,180);
*/

/*
// A6
// Initialisation
setPos(300,400);
changeColor(color.aqua);
setLineWidth(10);
faceUp();

// Réalisation
forward(100);
up();
forward(100);
down();
forward(200);
*/

/*
// A7
// Initialisation
setPos(200,300);
changeColor(color.green);
setLineWidth(10);
faceDown();

// Réalisation
forward(100);
arcLeft(50,180);
forward(50);
up();
forward(50);
down();
forward(100);
arcLeft(50,180);
forward(50);
*/

/*
// A8
// Initialisation
setPos(300,300);
changeColor(color.fuchsia);
setLineWidth(10);
faceUp();

// Réalisation
forward(50);
faceRight();
forward(100);
faceDown();
forward(200);
faceLeft();
forward(50);
up();
forward(50);
faceUp();
forward(50);
down();
forward(50);
*/

/*
// A9
// Initialisation
setPos(100,500);
changeColor(color.aqua);
setLineWidth(10);
faceRight();

// Réalisation
arcLeft(50,180);
arcRight(50,180);
forward(200);
arcRight(50,180);
arcLeft(50,180);
faceLeft();
forward(200);
*/

/*
// A10
// Initialisation
setPos(300,300);
changeColor(color.red);
setLineWidth(10);
faceRight();

// Réalisation
arcLeft(50,180);
changeColor("#00561B");
arcLeft(100,180);
changeColor("yellow");
arcLeft(200,180);
*/

// Série B
/*
// B1
// Initialisation
setPos(100,100);
changeColor(color.green);
setLineWidth(10);
faceRight();

// Réalisation

for (let i = 0 ; i < 4 ; i++) {
    forward(100);
    faceDown();
    forward(100);
    faceRight();
}
*/

/*
// B2
// Initialisation
setPos(50,300);
changeColor(color.red);
setLineWidth(10);
faceUp();

// Réalisation

for (let i=0;i<4;i++) {
    forward(50);
    faceRight();
    forward(50);
    faceDown();
    forward(50);
    faceRight(50);
    forward(50);
    faceUp();
}
*/

/*
// B3
// Initialisation
setPos(250,250);
changeColor(color.red);
setLineWidth(10);
faceRight();

// Réalisation
for (let i=0;i<4;i++) {
    forward(100);
    right(90);
}
*/

/*
// B4
// Initialisation
setPos(250,350);
changeColor(color.aqua);
setLineWidth(10);
faceRight();

// Réalisation
for (let i=0;i<3;i++) {
    forward(100);
    left(120);
}
*/

/*
// B5
// Initialisation
setPos(300,300);
changeColor(color.yellow);
setLineWidth(10);
faceUp();

// Réalisation
forward(100);
changeColor("red");
for (let i=0;i<3;i++) {
    forward(50);
    right(120);
}
*/

/*
// B6
// Initialisation
setPos(100,400);
changeColor(color.red)
setLineWidth(10);
faceRight();

//Réalisation
for (let i=0;i<2;i++) {
    forward(50);
    faceUp();
    forward(50);
    faceRight();
}
changeColor(color.yellow);
for (let i=0;i<3;i++) {
    forward(50);
    faceDown();
    forward(50);
    faceRight();
}
*/

/*
// B7
// Initialisation
setPos(300,300);
changeColor(color.red)
setLineWidth(10);
faceRight();

// Réalisation
for (let i=0;i<3;i++) {
    forward(100);
    left(120);
}
for (let i=0;i<4;i++) {
    forward(100);
    right(90);
}
*/

/*
// B8
// Initialisation
setPos(300,300);
changeColor(color.yellow)
setLineWidth(10);
faceRight();

// Réalisation
for (let i=0;i<8;i++) {
    forward(200);
    right(135);
}
*/

/*
// B9
// Initialisation
setPos(100,300);
changeColor(color.fuchsia)
setLineWidth(10);
faceRight();

// Réalisation
forward(50);
up();
forward(50);
down();
for (let i=0;i<3;i++) {
    forward(100);
    right(120);
}
*/

/*
// B10
// Initialisation
setPos(50,300);
changeColor(color.aqua)
setLineWidth(10);
faceRight();

// Réalisation
for (let i=0;i<8;i++) {
    forward(100);
    right(135);
}
up();
forward(200);
down();
changeColor(color.yellow);
for (let i=0;i<8;i++) {
    forward(200);
    left(135);
}
right(90);
up();
forward(100);
down();
changeColor(color.fuchsia);
for (let i=0;i<8;i++) {
    forward(100);
    right(135);
}
*/

/*
// B11
// Initialisation
setPos(200,300);
changeColor(color.red)
setLineWidth(10);
faceRight();

// Réalisation
for (let i=0;i<4;i++) {
    forward(100);
    left(90);
    forward(100);
    right(90);
    forward(100);
    right(90);
}
*/

/*
// B12
// Initialisation
setPos(200,300);
changeColor(color.red)
setLineWidth(10);
faceRight();

// Réalisation
for (let i=0;i<3;i++) {
    right(90);
    forward(50);
}
up();
changeColor(color.yellow);
forward(50);
down();
for (let i=0;i<3;i++) {
    forward(50);
    right(90);   
}
*/

/*
// B24
// Initiation
setPos(200,100);
changeColor(color.red)
setLineWidth(10);
faceRight();

// Réalisation
for (let i=0;i<8;i++) {
    forward(100);
    left(135);
}
forward(50);
changeColor(color.aqua);
for (let i=0;i<3;i++) {
    right(135);
    forward(100);
    left(135);
    forward(50)
}
forward(50);
right(90);
for (let i=0;i<2;i++) {
    forward(25);
    left(90);
}
forward(25);
right(90);
forward(50);
for (let i=0;i<3;i++) {
    forward(50);
    left(135);
    forward(100);
    right(135);
}
*/

// Série C
/*
// C6
// Initiation
setPos(300,300);
changeColor(color.yellow)
setLineWidth(10);
faceRight();

// Réalisation
for (let i=0;i<3;i++) {
    for (let j=0;j<4;j++) {
        forward(100);
        left(120);
    }
    forward(100);
}
*/

/*
// C8
// Initialisation
setPos(300,550);
changeColor(color.aqua)
setLineWidth(10);
faceUp();

//Réalisation
forward(200);
right(45);
for (let i=0;i<4;i++) {
    changeColor(color.red);
    for (let j=0;j<3;j++) {
        forward(100);
        right(120);
    }
    changeColor(color.yellow);
    forward(100);
    left(90);
}
*/

// Série D
/*
// D9
// Initialisation
setPos(300,300);
changeColor(color.blue);
setLineWidth(10);
faceRight();
let Length=50;
let Incr=50;

//Réalisation
for (let i=0;i<8;i++) {
    forward(Length);
    right(120);
    Length = Length + Incr;
}
*/

/*
// D12 
// Initialisation
setPos(300,300);
changeColor(color.aqua);
setLineWidth(10);
faceRight();
let Length = 50;
let Incr = 50;

// Réalisation
for (let i=0;i<6;i++) {
    for (let j=0;j<4;j++) {
        forward(Length);
        left(120);
    }
    right(60);
    Length = Length + Incr;
}
*/

/*
// D14
// Initialisation
setPos(50,300);
changeColor(color.aqua)
setLineWidth(10);
faceRight();
left(45);
let Lenght = 40;
let Incr = 40;

// Réalisation
for (let i=0;i<2;i++) {
    Length = 50;
    for (let j=0;j<4;j++) {
        forward(Length);
        right(90);
        forward(Length);
        left(90);
        Length = Length + Incr
    }
    right(180);
}
*/

/*
// D16
// Initialisation
setPos(250,250);
changeColor(color.aqua);
setLineWidth(10);
faceDown();
let ray = 50;
let length = 50;
let incr = 50;

//Réalisation
forward(length*2);
right(120);
forward(length)
faceLeft();
for (let i=0;i<3;i++) {
    forward(length);
    right(90);
}
left(120);
forward(length);
faceRight();
up();
forward(length);
down();
changeColor(color.yellow);
for (let i=0;i<3;i++) {
    arcRight(ray,180);
    faceUp();
    up();
    forward((ray*2)+50);
    ray = ray + incr;
    faceRight();
    down();
}
*/
/*
// Série E
// E10
// Initialisation
setPos(50,550);
changeColor(color.aqua);
setLineWidth(10);
faceRight();
up();

function croix() {
    down();
    for(let i=0;i<4;i++) {
        for(let j=0;j<2;j++) {
            forward(50);
            right(90);
        }
        forward(50);
        left(90);
    } 
    up();  
}

// Réalisation

for (let i=0;i<3;i++) {
   croix();
   forward(200);
   faceUp();
   croix();
   forward(200);
   faceRight();
   shiftColor(0.3);
}
*/


// Série BONUS 
/*
// BONUS 1
// Initialisation
let iter=100;
let length=15;
setPos((300-(length/2)),590);
changeColor(color.red);
setLineWidth(5);
faceRight();


for (let i=3;i<iter;i++) {
    for (let j=0;j<i;j++) {
        forward(length);
        left(360/i);
    }
    shiftColor(1/iter);
}
*/
/*
// BONUS 2
let iter=100;
let length=200;
setPos(300,300);
changeColor(color.red);
setLineWidth(5);
faceUp();

for (let i=0;i<iter;i++) {
    forward(length);
    setPos(300,300);
    right(360/iter);
    shiftColor(1/iter);
   
}
*/

/*
// BONUS 3
let length=1500;
let height=50;
let x0=100;
let y0=300;
setPos(x0,y0);
changeColor(color.red);
setLineWidth(5);
faceUp();

for (let i=0;i<length;i++) {
    forward(height);
    setPos((x0+i*0.1),y0);
    shiftColor(1/1500);

}
*/

// BONUS ??

let length=100;
let height=50;
let x0=300;
let y0=300;
setPos(x0,y0);
changeColor(color.red);
setLineWidth(5);
faceUp();

if (keypress == ) {

}